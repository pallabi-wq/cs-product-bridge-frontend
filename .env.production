# Production API base — points to the Supabase Edge Function.
# The function is named "api", so all routes are prefixed with /api
# (Supabase prepends the function name to the path).
VITE_API_BASE=https://xkbgloaanzirzasldwro.supabase.co/functions/v1/api
